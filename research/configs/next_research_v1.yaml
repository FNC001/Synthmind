config_version: next_research_v1
allow_test_eval: false
allow_selector_update: false
max_trials: 6
seed: 42
candidate_budget: candidate_budget_v1
phases: [audit, freeze, reproduce, attribute, baseline]
default_branch_policy:
  use_attribution_when_available: true
  fallback_branch: GRV
paths:
  legacy_evaluation_root: outputs/evaluation.local_bak_20260612
  legacy_runs_root: runs.local_bak_20260612
  reports_root: reports
  artifacts_root: artifacts

