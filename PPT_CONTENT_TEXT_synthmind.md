# synthmind 材料合成路线预测系统 PPT 内容文字版

## 封面

**标题：** 基于晶体结构的材料合成路线智能预测方法  
**副标题：** 前驱体集合预测、合成工艺条件推荐与 GNoME 候选材料批量筛选  
**汇报内容：** 数据库规模、前驱体预测算法、给定条件推荐方法、结果总结、GNoME 数据库应用情况

---

## 第一部分 数据库规模介绍

### 第 1 页：研究目标与任务定义

本项目面向无机材料合成路线预测任务，目标是从目标材料的晶体结构和化学组成出发，自动推荐可能的合成路线。系统主要包含两个核心预测环节：

1. **Stage2：结构/组成 → 前驱体集合预测**  
   给定目标晶体结构和组成，预测可用于合成该材料的一组前驱体化合物。

2. **Stage3：结构/组成/前驱体 → 合成工艺条件预测**  
   在给定候选前驱体的基础上，预测温度、时间、气氛、溶剂、合成方法等工艺条件。

最终系统输出的是一条完整合成路线，包括：

- 目标材料；
- 推荐前驱体集合；
- 推荐反应方法；
- 推荐合成温度；
- 推荐合成时间；
- 推荐反应气氛；
- 推荐溶剂或工艺环境。

---

### 第 2 页：主训练数据库规模

当前用于公平评估的主数据库采用 **method-stratified** 拆分，即按照反应方式分层划分训练集、验证集和测试集，以避免模型只在单一固相路线中表现较好。

| 数据划分 | 样本数 | 用途 |
|---|---:|---|
| 训练集 train | 29,208 | 模型训练 |
| 验证集 validation | 3,658 | 参数选择、早停、排序校准 |
| 测试集 test | 3,664 | 最终公平评估 |
| 合计 | 36,530 | 多反应方式结构化合成数据 |

数据库覆盖的主要信息包括：

- 目标材料组成和结构描述符；
- 目标材料对应的前驱体集合；
- 反应方式；
- 合成温度；
- 合成时间；
- 反应气氛；
- 溶剂或工艺介质；
- 文献来源和样本标识。

---

### 第 3 页：反应方式覆盖情况

主测试集覆盖 11 类主要反应方式，能够反映模型在不同工艺路线上的泛化能力。

| 反应方式 | 测试集样本数 |
|---|---:|
| solid_state 固相法 | 1,142 |
| solution 溶液法 | 1,102 |
| other 其他 | 737 |
| melt_arc 熔融/电弧法 | 224 |
| hydro_solvothermal 水热/溶剂热 | 191 |
| precipitation 沉淀法 | 128 |
| flux_molten_salt 熔盐法 | 68 |
| thermal_decomposition 热分解 | 32 |
| mechanochemical 机械化学 | 28 |
| sol_gel 溶胶凝胶 | 7 |
| combustion 燃烧法 | 5 |

其中，固相法、溶液法和熔融/电弧法样本量最大，数据质量和规律性相对更好，因此进一步构建了核心反应方式子集。

---

### 第 4 页：核心反应方式子集

核心子集只包含三类主流且样本量较大的反应方式：

- solid_state 固相法；
- solution 溶液法；
- melt_arc 熔融/电弧法。

| 数据划分 | 总样本数 | solid_state | solution | melt_arc |
|---|---:|---:|---:|---:|
| train | 19,722 | 8,899 | 8,940 | 1,883 |
| validation | 2,441 | 1,061 | 1,124 | 256 |
| test | 2,468 | 1,142 | 1,102 | 224 |

核心子集用于训练当前最稳定的可部署模型。其优势在于：

- 样本量更集中；
- 反应机理更清晰；
- 标签噪声相对更低；
- 适合做实际候选材料的批量路线推荐。

---

### 第 5 页：前驱体标签与数据清洗

原始前驱体数据存在大量同义写法、别名、水合物格式差异、相态后缀和溶液后缀。为提高模型可学习性，系统对前驱体标签进行了规范化处理。

| 前驱体数据项目 | 数量 |
|---|---:|
| 原始前驱体标签 | 4,520 |
| canonical v2 标签 | 4,440 |
| canonical v4 标签 | 4,414 |
| ontology 前驱体标签 | 4,440 |
| family 类别数 | 13 |
| 元素类别数 | 107 |

规范化内容包括：

- 统一水合物符号；
- 修正 `H20` / `H2O` 等错误；
- 去除 `(aq)`、`powder`、`solution` 等无意义后缀；
- 合并常见别名；
- 使用化学式解析校验前驱体组成；
- 构建前驱体 ontology，标注 oxide、carbonate、nitrate、halide、elemental 等类别。

---

## 第二部分 前驱体合成算法介绍

### 第 6 页：Stage2 前驱体预测任务

Stage2 的任务是：给定目标材料结构和组成，预测一组能够合成该材料的前驱体集合。

该任务具有三个难点：

1. **输出是集合，不是单标签。**  
   前驱体没有固定顺序，且一个目标材料可能有多条可行路线。

2. **前驱体标签存在长尾和 OOV。**  
   测试集中会出现训练集中未见过的前驱体或别名。

3. **化学约束很强。**  
   前驱体必须覆盖目标元素，同时不能引入明显无关元素。

因此，系统没有采用单一闭集分类模型，而是采用“模型预测 + 检索候选 + 化学规则生成 + 集合排序”的混合策略。

---

### 第 7 页：前驱体预测算法流程

Stage2 算法流程如下：

1. **晶体结构特征提取**  
   从 POSCAR/CIF 中提取组成、元素比例、晶格参数、密度、近邻距离、配位统计等结构描述符。

2. **多标签 MLP 前驱体预测**  
   使用神经网络预测训练集中已出现前驱体的概率分布。

3. **set-size predictor**  
   预测前驱体集合大小，避免简单阈值导致候选过多或过少。

4. **检索候选生成**  
   根据结构、组成和反应方式检索相似历史样本，复用其真实前驱体集合。

5. **开放式化学候选生成**  
   根据目标元素和 precursor family 生成氧化物、碳酸盐、硝酸盐、卤化物、单质等候选。

6. **候选集合 beam search**  
   将单个前驱体组合为候选集合，并进行化学约束过滤。

7. **集合级排序与校准**  
   使用验证集校准分数，使 top1 推荐尽可能接近真实路线。

---

### 第 8 页：Precursor Ontology 与开放式生成

为了缓解闭集标签分类无法处理未见前驱体的问题，系统构建了 precursor ontology。

每个前驱体被解析为：

- 化学式；
- 元素组成；
- 可提供的目标元素；
- 前驱体族；
- 阴离子族；
- 是否为水合物；
- 是否为氧化物、碳酸盐、硝酸盐、卤化物、单质等。

基于 ontology，前驱体预测被拆解为两个层次：

1. **元素来源预测**  
   判断目标材料中的每个元素应该由哪类前驱体提供。

2. **具体前驱体生成**  
   在对应元素和 family 下选择历史前驱体，或根据化学模板生成新前驱体。

例如：

- Li 对应 carbonate family 时，可生成 `Li2CO3`；
- Mn 对应 oxide family 时，可生成 `MnO2`、`Mn2O3` 或 `Mn3O4`；
- 熔融/电弧法中金属元素优先生成单质前驱体。

---

### 第 9 页：化学约束后处理

在对 GNoME 候选材料进行批量预测时，发现原始 MLP top-k 前驱体存在明显化学不合理情况，例如漏掉目标元素或引入无关金属。

因此，系统增加了化学约束后处理：

1. 保留模型输出中化学合理的前驱体；
2. 检查目标元素是否全部被覆盖；
3. 对缺失元素使用已知前驱体词表补齐；
4. 若训练标签中不存在合适前驱体，则使用开放式规则生成；
5. 禁止引入明显无关的金属、稀土或主族阳离子。

修正前后对比：

| 项目 | 原始模型输出 | 化学约束修正后 |
|---|---:|---:|
| GNoME 推荐 top1 条目数 | 92,310 | 92,310 |
| 目标来源元素完全覆盖 | 36.15% | 100% |
| 缺失来源元素条目 | 大量存在 | 0 |
| 无关来源元素条目 | 大量存在 | 0 |
| 使用开放式补齐条目 | - | 23,874 |

因此，实际使用时应查看 `_chem_checked.csv` 文件中的 `precursors_text` 列。

---

## 第三部分 给定条件推荐方法

### 第 10 页：Stage3 工艺条件预测任务

Stage3 的任务是：在给定目标材料结构、组成和候选前驱体集合的情况下，推荐合成工艺条件。

预测内容包括：

- 合成方法类型；
- 合成温度；
- 合成时间；
- 反应气氛；
- 溶剂或反应介质。

为了保证数据口径一致，系统对工艺条件进行了单位归一化：

- 温度统一为摄氏度；
- 时间统一为小时；
- 气氛统一为 `air`、`ar`、`n2`、`o2`、`vacuum` 等标准标签；
- 溶剂统一为 water、ethanol、methanol、dmf、dmso 等标准标签。

---

### 第 11 页：反应方式专家模型

不同反应方式的工艺条件分布差异很大。例如：

- 固相法通常温度较高、时间较长；
- 溶液法温度较低，常涉及溶剂；
- 熔融/电弧法常以单质为前驱体，气氛控制更重要。

因此，Stage3 采用 LightGBM method experts：

- 对核心反应方式分别训练专家模型；
- 连续变量使用回归模型预测；
- 离散变量使用分类模型预测；
- 对样本不足或不确定情况使用全局模型回退。

当前核心模型覆盖三类方法：

- 固相法；
- 溶液法；
- 熔融/电弧法。

在 GNoME 批量预测中，每个材料都会生成三条候选路线，并额外给出一个推荐 top1 路线。

---

### 第 12 页：完整路线推荐逻辑

完整路线推荐不仅要预测前驱体，也要匹配工艺条件。

系统的路线推荐逻辑包括：

1. 对目标结构生成候选前驱体集合；
2. 对每个候选前驱体集合分别预测工艺条件；
3. 对不同反应方式分别计算条件；
4. 综合前驱体置信度、反应方式先验和条件预测结果；
5. 输出 top1 推荐路线和多方法候选路线。

当前输出文件中包含：

- `reaction_method`：机器标签形式的反应方式；
- `synthesis_method_cn`：中文合成方法；
- `precursors_text`：化学检查后的前驱体；
- `pred_temperature_c`：预测温度；
- `pred_time_h`：预测时间；
- `pred_atmosphere`：预测气氛；
- `pred_solvent`：预测溶剂；
- `synthesis_route_text_cn`：整理后的一句话合成路线。

---

## 第四部分 结果总结

### 第 13 页：Stage2 前驱体预测结果

Stage2 的主要评价指标为：

- **top1 exact**：排序第一的前驱体集合是否与真实集合完全一致；
- **top10 exact**：前 10 个候选中是否包含真实集合；
- **top200 / top500 exact**：候选池覆盖能力；
- **Jaccard / F1**：候选集合与真实集合的部分匹配程度。

在 method-stratified 公平测试集上，前驱体预测持续提升：

| 版本 | top1 exact | top10 exact | top200 exact | top500 exact |
|---|---:|---:|---:|---:|
| v2 baseline | 33.95% | 60.81% | 70.41% | 73.06% |
| v3 ontology/open-vocab | 35.94% | 60.04% | 73.42% | 76.12% |
| v5 final | 39.47% | 63.35% | 77.02% | 80.24% |
| core-method best | 46.15% | 69.17% | 81.69% | 85.33% |

核心子集表现更好，说明在主流且数据充分的反应方式中，前驱体集合预测已经具有较高候选覆盖能力。

---

### 第 14 页：Stage3 工艺预测结果

Stage3 的评价指标包括：

- 温度 MAE；
- 时间 MAE；
- 气氛分类准确率；
- strict condition success；
- relaxed condition success。

其中：

- strict condition：温度误差 ≤100 °C，时间误差 ≤24 h，气氛正确；
- relaxed condition：温度误差 ≤200 °C，时间误差 ≤48 h，气氛正确。

核心反应方式 Stage3 模型结果：

| 设置 | 温度 MAE | 时间 MAE | 气氛准确率 | strict cond | relaxed cond |
|---|---:|---:|---:|---:|---:|
| core-only retrain | 200.50 °C | 29.60 h | 55.35% | 11.02% | 16.61% |

如果只在温度、时间和气氛都有标注的可评估样本上计算：

| 口径 | strict condition | relaxed condition |
|---|---:|---:|
| 所有 core test 样本为分母 | 11.02% | 16.61% |
| 仅可评估样本为分母 | 20.86% | 31.44% |

这说明工艺条件预测受缺失标签、实验噪声和多模态路线影响较大，是后续提升重点。

---

### 第 15 页：完整路线结果与当前瓶颈

完整路线评价要求前驱体和合成条件同时正确，因此难度显著高于单独 Stage2 或 Stage3。

当前完整路线结果：

| 评估设置 | top1 strict route | top1 relaxed route | top10 strict route | top10 relaxed route |
|---|---:|---:|---:|---:|
| method-stratified top10，全方法 | 3.11% | 6.39% | 5.90% | 11.35% |
| Stage35 rerank，全方法 | 3.49% | 6.85% | 5.90% | 11.35% |
| old/core Stage35 | 8.18% | 16.73% | 10.97% | 21.75% |

当前主要瓶颈：

1. 前驱体预测中仍存在 OOV 和长尾路线；
2. 工艺条件具有多解性，同一材料可由不同温度/时间路线合成；
3. 气氛和溶剂标签噪声较大；
4. 工艺文本中的隐含信息尚未充分利用；
5. topK 条件候选中存在较好路线，但排序器还不能稳定排到第一。

---

### 第 16 页：阶段性结论

当前系统已经形成完整技术闭环：

1. 建立了结构化合成数据库；
2. 完成了前驱体标签规范化和 ontology 构建；
3. 实现了结构到前驱体集合预测；
4. 实现了前驱体到工艺条件推荐；
5. 支持对大规模候选晶体库进行批量合成路线预测；
6. 对 GNoME 候选结构完成了 9 万余条材料的批量推理。

从结果看：

- 前驱体候选覆盖能力已经较强，核心方法 top500 exact 达到 85.33%；
- top1 前驱体预测仍有提升空间，核心方法 top1 为 46.15%；
- 工艺条件预测仍是完整路线成功率的主要瓶颈；
- 后续应重点提升条件候选排序、多模态工艺生成和高频前驱体清洗。

---

## 第五部分 GNoME 数据库情况

### 第 17 页：GNoME 数据库应用目的

GNoME 数据库提供了大规模候选晶体结构，可作为材料发现和合成路线推荐的输入来源。

本项目使用 GNoME 候选结构的目的包括：

1. 将已训练的 synthmind 模型应用于大规模未知候选材料；
2. 对候选材料批量生成前驱体和合成条件；
3. 为后续实验筛选提供可操作的合成路线建议；
4. 识别哪些材料更适合固相法、溶液法或熔融/电弧法；
5. 建立“候选结构 → 合成路线”的自动化筛选流程。

---

### 第 18 页：本次 GNoME 批量预测规模

本次对本地 GNoME selected 目录进行了全量预测。

输入目录：

`/Users/lihonglin/Desktop/Syn_DP/synthmind/Genome/selected`

预测规模：

| 项目 | 数量 |
|---|---:|
| CIF 文件数 | 92,310 |
| 成功解析 | 92,310 |
| 解析失败 | 0 |
| 每个材料候选路线数 | 3 |
| 总候选路线数 | 276,930 |
| 推荐 top1 路线数 | 92,310 |

每个材料生成三类路线：

- 固相法；
- 溶液法；
- 熔融/电弧法。

---

### 第 19 页：GNoME 推荐路线分布

在推荐 top1 路线中，各方法分布如下：

| 推荐方法 | 数量 |
|---|---:|
| 熔融/电弧法 melt_arc | 58,630 |
| 溶液法 solution | 25,600 |
| 固相法 solid_state | 8,080 |

预测工艺条件范围：

| 条件 | 均值 | 中位数 | 最小值 | 最大值 |
|---|---:|---:|---:|---:|
| 温度 | 671.37 °C | 766.65 °C | 23.73 °C | 1409.92 °C |
| 时间 | 52.67 h | 29.05 h | 3.96 h | 270.21 h |

需要注意的是，GNoME 中许多候选材料包含金属间化合物、卤化物、硫族化合物和多元素复杂组成，因此熔融/电弧法在推荐中占比较高。

---

### 第 20 页：GNoME 前驱体结果修正情况

初始模型输出中，原始前驱体集合存在化学覆盖不足问题，因此对 GNoME 结果进行了化学约束修正。

修正后的输出文件：

- `genome_selected_recommended_top1_chem_checked.csv`
- `genome_selected_predictions_chem_checked.csv`

修正结果：

| 文件 | 行数 | 通过检查 | 缺失来源元素 | 无关来源元素 | 使用开放式补齐 |
|---|---:|---:|---:|---:|---:|
| 推荐 top1 | 92,310 | 92,310 | 0 | 0 | 23,874 |
| 三路线全表 | 276,930 | 276,930 | 0 | 0 | 71,622 |

实际使用时应以 `_chem_checked.csv` 中的 `precursors_text` 为准，而不是原始模型输出。

---

### 第 21 页：GNoME 输出文件说明

本次 GNoME 批量预测主要输出如下：

| 文件 | 说明 |
|---|---|
| `genome_selected_recommended_top1_chem_checked.csv` | 每个材料推荐一条化学检查后的合成路线 |
| `genome_selected_predictions_chem_checked.csv` | 每个材料保留三种反应方式候选路线 |
| `summary.json` | 批量预测统计摘要 |
| `PREDICTION_REPORT.md` | GNoME 批量预测说明 |
| `PRECURSOR_TEXT_CHECK_REPORT.md` | 前驱体化学检查报告 |

重点查看字段：

- `formula`：目标材料组成；
- `synthesis_method_cn`：中文合成方法；
- `precursors_text`：修正后的前驱体；
- `pred_temperature_c`：预测温度；
- `pred_time_h`：预测时间；
- `pred_atmosphere`：预测气氛；
- `pred_solvent`：预测溶剂；
- `synthesis_route_text_cn`：中文合成路线摘要。

---

### 第 22 页：总结与下一步工作

本项目已完成从训练数据库构建、前驱体预测、工艺条件推荐到 GNoME 候选材料批量推理的完整流程。

当前优势：

- 数据库规模较大，覆盖多种反应方式；
- 前驱体预测具备候选池覆盖优势；
- 已引入 ontology 和开放式生成，能处理部分未见前驱体；
- 支持大规模 CIF 批量推理；
- 对 GNoME 92,310 个候选结构完成合成路线推荐。

下一步优化方向：

1. 加强高频前驱体 alias 和 OOV 清洗；
2. 引入更强的前驱体 set-level reranker；
3. 针对 Stage3 训练多模态条件候选生成模型；
4. 将合成文本描述引入工艺条件预测；
5. 对 GNoME 输出进行人工优先级筛选，形成实验验证候选清单。

