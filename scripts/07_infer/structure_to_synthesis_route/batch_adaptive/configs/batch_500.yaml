project_root: /Users/wyc/SynPred

pipeline_v3_dir: /Users/wyc/SynPred/scripts/07_infer/structure_to_synthesis_route/pipeline
pipeline_v3_config: /Users/wyc/SynPred/scripts/07_infer/structure_to_synthesis_route/pipeline/configs/full_route_stage3.yaml

# 输入批量 POSCAR 总目录
# 里面可以直接放 POSCAR 文件，也可以放很多子目录
batch_poscar_dir: /Users/wyc/SynPred/data/infer/batch_500_poscars

# 每个 case 的输入会自动复制到：
# data/infer/<case_id>/poscars/
case_input_root: /Users/wyc/SynPred/data/infer

# 每个 case 的 pipeline_v3 输出仍然使用：
# outputs/inference/<case_id>/
case_output_root: /Users/wyc/SynPred/outputs/inference

# adaptive batch 总输出目录
batch_output_root: /Users/wyc/SynPred/outputs/batch_adaptive

batch_name: batch_500

device: cpu

# 第一层从哪个步骤开始
pipeline_start_from: make_infer_split

# 如果 case 已经完成，是否跳过
resume: true

# 诊断阈值
audit:
  min_condition_support_score: 0.70
  low_condition_support_score: 0.50
  allow_minor_warning_as_review: true
